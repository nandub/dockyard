# Security

This is a local example package. Do not commit production database passwords or deployment-specific values.

For real deployments, provide the PostgreSQL password from an environment file, CI/CD secret, or external secret manager rather than committing it to source control.

Report security issues to the repository owner privately.
